export default function AboutComponent() {
    return (
        <div style={{display:'flex', flexDirection:'column', alignItems:'center'}}>
            <h2>About Page</h2>
            <p>This is the about page loaded lazily.</p>
        </div>
    )
}